-- ============================================
-- SEED DATA PARA DESENVOLVIMENTO LOCAL
-- ============================================
-- Este arquivo é executado quando você roda `supabase db reset`
-- Use para inserir dados de teste

-- Exemplo: Criar usuário admin de teste (apenas para desenvolvimento local)
-- INSERT INTO auth.users (id, email, encrypted_password, email_confirmed_at, created_at, updated_at)
-- VALUES (
--   gen_random_uuid(),
--   'admin@test.local',
--   crypt('senha123', gen_salt('bf')),
--   NOW(),
--   NOW(),
--   NOW()
-- );

-- Adicione seus dados de seed aqui
-- Exemplo: clientes, revendas, etc.

